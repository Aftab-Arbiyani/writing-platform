/**
 * Public surface of the search module (docs 16 §5.2 — one barrel per backend
 * module). Feature modules import search integration points from here.
 */
export { SearchModule } from './search.module';
export { SearchService } from './search.service';
