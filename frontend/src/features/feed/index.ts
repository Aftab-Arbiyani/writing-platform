/**
 * Public surface of the feed feature — the Home/Feed page (imported by the lazy `feed` route)
 * and the reusable `PieceCard` (one component per concept, docs/06 §10.3).
 */
export { FeedPage } from './pages/feed-page';
export { PieceCard } from './components/piece-card';
