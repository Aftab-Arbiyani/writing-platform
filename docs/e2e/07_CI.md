# E2E 07 — Continuous Integration

> **Status:** Binding. Defines the `web-e2e.yml` GitHub Actions workflow that runs the browser suite in
> CI — mirroring the existing backend `e2e.yml` (same Postgres/Redis/MinIO wiring + migrations + seed),
> adding Playwright browser install, both built apps served, the three-engine matrix, sharding, and
> failure-diagnostic artifacts. Also sets the **gate policy** (when E2E becomes blocking).

---

## 1. What we reuse from the existing `e2e.yml`

The backend `e2e.yml` already establishes the real-infra pattern we copy verbatim:

- **Service containers:** `postgres:16-alpine`, `redis:7-alpine`, MinIO run as a background container
  with a created `qalam-media` bucket (GH service containers can't set MinIO's `server` command).
- **Env baseline:** `DATABASE_URL`, `REDIS_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, the `S3_*`
  block, `SMTP_URL`, `SCHEDULER_ENABLED: 'false'` (cron must not fire mid-suite; workers stay on so
  queue-backed flows still process).
- **Toolchain:** `pnpm/action-setup@v4` (9.12.0), `actions/setup-node@v4` (node 24, pnpm cache),
  `pnpm install --frozen-lockfile`.

`web-e2e.yml` starts from that job and adds the browser + app layer. Where backend `e2e.yml` runs
`pnpm test:e2e` (supertest against the API), `web-e2e.yml` builds+serves both apps and runs Playwright.

**Additions over backend e2e:** **Mailpit** as a container (`axllent/mailpit`, SMTP `:1025` + API `:8025`)
so email flows are testable, pointing `SMTP_URL` at it.

---

## 2. Job shape (`.github/workflows/web-e2e.yml`)

```yaml
name: Web E2E

on:
  workflow_dispatch:
  push:
    branches: [main, develop] # non-blocking on merge, matching current e2e.yml posture (until §6)
  # pull_request:              # ENABLED at Phase 2 exit — see §6 gate policy

concurrency:
  group: web-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web-e2e:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit] # all three, every run (docs/e2e/00 §5.2)
        shard: [1, 2] # split specs per browser for wall-clock (§4)
    services:
      postgres: { image: postgres:16-alpine, ... } # identical to e2e.yml
      redis: { image: redis:7-alpine, ... }
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://qalam:qalam@localhost:5432/qalam
      REDIS_URL: redis://localhost:6379
      JWT_ACCESS_SECRET: ci-access-secret-not-a-real-secret-000000
      JWT_REFRESH_SECRET: ci-refresh-secret-not-a-real-secret-00000
      S3_ENDPOINT: http://localhost:9000
      S3_REGION: us-east-1
      S3_BUCKET: qalam-media
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      SMTP_URL: smtp://localhost:1025 # → Mailpit
      SCHEDULER_ENABLED: 'false'
      CI: 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.12.0 }
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: pnpm }

      - name: Start MinIO + create bucket # copied from e2e.yml
        run: |
          docker run -d --name minio -p 9000:9000 \
            -e MINIO_ROOT_USER=minioadmin -e MINIO_ROOT_PASSWORD=minioadmin \
            minio/minio server /data
          for i in $(seq 1 30); do curl -sf http://localhost:9000/minio/health/ready && break || sleep 2; done
          aws --endpoint-url http://localhost:9000 s3 mb s3://qalam-media || true
        env:
          {
            AWS_ACCESS_KEY_ID: minioadmin,
            AWS_SECRET_ACCESS_KEY: minioadmin,
            AWS_DEFAULT_REGION: us-east-1,
          }

      - name: Start Mailpit
        run: docker run -d --name mailpit -p 1025:1025 -p 8025:8025 axllent/mailpit

      - run: pnpm install --frozen-lockfile

      - name: Build shared packages + both apps # CI serves BUILT output (docs/e2e/01 §4)
        run: pnpm --filter "@qalam/*" build && pnpm --filter frontend build && pnpm --filter admin build

      - name: Start backend, migrate, seed
        run: |
          pnpm --filter backend build
          node backend/dist/main.js &             # or `pnpm --filter backend start:prod`
          e2e/scripts/wait-health.sh http://localhost:4000/health
          pnpm --filter backend migration:run
          pnpm --filter backend seed
          pnpm --filter backend seed:e2e          # the e2e-fixtures seed (docs/e2e/04 §3)

      - name: Install Playwright browser
        run: pnpm --filter e2e exec playwright install --with-deps ${{ matrix.browser }}

      - name: Run E2E ( ${{ matrix.browser }} shard ${{ matrix.shard }} )
        run: >-
          pnpm --filter e2e exec playwright test
          --project=frontend-${{ matrix.browser }} --project=admin-${{ matrix.browser }}
          --shard=${{ matrix.shard }}/2
        env:
          E2E_API_URL: http://localhost:4000/api/v1

      - name: Upload report + traces
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}-shard${{ matrix.shard }}
          path: |
            e2e/playwright-report/
            e2e/test-results/
          retention-days: 14
```

> The `webServer` block in `playwright.config.ts` serves the two apps via `vite preview` on the built
> output (CI branch of the config, [01 §5](./01_Architecture.md)); the job above only needs the backend
>
> - infra up first. `--with-deps` installs the OS libraries WebKit/Firefox need on the runner.

---

## 3. The browser matrix — all three, managed for cost

The matrix runs **chromium × firefox × webkit**, and within each, both app projects. `fail-fast: false`
so a WebKit failure doesn't cancel the Chromium leg — we want the full cross-engine picture on every run.

**Cost trade-off (acknowledged):** three engines ≈ 3× the compute of a single-engine run. We accept it
per the explicit "in-depth, all three browsers every phase" decision ([00 §5.2](./00_Overview.md)), and
manage wall-clock with sharding (§4) rather than by dropping an engine.

---

## 4. Sharding

Each `(browser)` leg splits its specs into `--shard=N/2` across two matrix entries, so the six real
projects run as **6 legs × 2 shards = 12 parallel jobs**. As the suite grows past Phase 2, bump the
shard count (`shard: [1,2,3,4]` + `--shard=N/4`). **Why shard instead of more workers-per-job:** GH
runners are small (2 vCPU); three browser engines already saturate one runner's workers. Horizontal
shards give real parallelism across runners.

The HTML report is a **blob report** per shard; a final `merge-reports` step (optional, Phase 2+) merges
all shards/browsers into one combined HTML report artifact.

---

## 5. Failure diagnostics (the payoff)

Per [01 §5](./01_Architecture.md) config: `trace: 'on-first-retry'`, `screenshot: 'only-on-failure'`,
`video: 'retain-on-failure'`, HTML reporter + `github` reporter (annotates the PR/commit with failures).
On any failure the uploaded artifact contains:

- **Trace** (`.zip`) — open with `pnpm --filter e2e exec playwright show-trace <file>`: a clickable
  timeline with DOM snapshots, network, console, and the exact failing step.
- **Screenshot** at the point of failure.
- **Video** of the whole failing test.

Artifacts are named per browser+shard and retained 14 days. **Why on-first-retry, not always:** traces
are large; capturing them only when a test misbehaves keeps artifact storage sane while still catching
every real failure.

---

## 6. Gate policy — when E2E blocks a PR

Matching the current non-blocking posture of backend `e2e.yml` (docs 18: "e2e becomes a blocking gate
once infra is automated"):

| Stage            | Triggers                              | Blocking?          |
| ---------------- | ------------------------------------- | ------------------ |
| Phase 1          | `workflow_dispatch`, `push: main`     | **No** (advisory)  |
| **Phase 2 exit** | + `pull_request`                      | **Yes — required** |
| Phase 3–4        | `pull_request`, `push: main`, nightly | **Yes — required** |

The switch to blocking happens only after Phase 2 is green three consecutive runs with zero quarantine
([06 §4](./06_PhasePlan.md)) — **counted as §6.2 defines it, which is not what this sentence used to
be read as**. **Why gate at Phase 2, not Phase 1:** Phase 1 is the harness bring-up; gating on a
suite that's still stabilizing trains people to bypass it. Gate once it's trustworthy.

A **nightly** scheduled run (`schedule: cron`) runs the full suite (all phases, no shard filter) once
Phase 3 lands, catching flake and environment drift that a PR-scoped run might miss.

### 6.1 Promotion status (2026-07-27) — why the gate is still open

All five phases are **implemented and live-validated locally**, but the gate is still advisory
because **the workflow had never actually executed**. Two blockers, both now fixed in the workflow:

1. **The trigger was unreachable.** `push: branches: [main]` was the only real trigger, and this
   repo's work lives on `develop` (`main` holds the README commit alone) — so no push ever matched.
   `workflow_dispatch` did not help either: GitHub only surfaces the dispatch button for workflows
   present on the **default branch**, and `main` carries no workflow files. Fixed by adding
   `develop` to the push branches (a `push` event uses the pushed commit's own copy of the file).
2. **The backend was started before its schema existed.** The "build + start backend, migrate,
   seed" step booted `node backend/dist/main.js` first and migrated afterwards, but
   `SettingsService.onModuleInit` writes setting + feature-flag definitions to the DB at bootstrap.
   Against an unmigrated database the process dies on boot and the health wait burns its full
   120s before failing the job — in **both** jobs. Fixed by migrating and seeding before start.

~~**Remaining promotion steps** (in order): push `develop` → confirm three consecutive green runs of
both `web-e2e` and `web-e2e-visual` with zero quarantine → uncomment the `pull_request` trigger →
mark both jobs required in branch protection ([docs/17 §4.1](../17_GitWorkflow.md)).~~ **Superseded
by §6.2 — as written, this step was UNREACHABLE.** The release-gate reference itself is already
written up in [docs/22 — Browser E2E](../22_ReleaseChecklist.md).

### 6.2 What "three consecutive green runs" counts — answered 2026-09-02

**This was the last thing standing between the suite and a required PR gate, and it was a definition
problem rather than an engineering one.** Recorded here, at the criterion's source, because the
previous omission in this very section is what let a wrong reading survive: §6.1's promotion step
required `web-e2e-visual` to be green three times running, and by 2026-08-31 that job **no longer
runs on a push at all**. A criterion that names a job the gate never executes cannot be satisfied,
and nothing in the sentence said so.

**The criterion, restated.**

> Three **consecutive** runs of `web-e2e.yml` triggered by a **push to `develop`**, in each of which
> **all four `web-e2e` matrix jobs** (`chromium`/`firefox` × shards 1–2) conclude `success`, with
> zero quarantine. Counted from the first such run **after `41b2b49`**. One red job resets the count
> to zero.

**Why the four functional jobs and nothing else** — the owner decision, taken 2026-09-02:

- **Count the shape that will become the gate.** What is being promoted is the 4-job push matrix
  into a required PR check. Evidence about a matrix the gate will not run is evidence about
  something else. This also disposes of run #30's red cleanly rather than by exception: it was a
  **webkit** job, webkit came off the push matrix in `11f5efb`, and its cause was the missing root
  `timeout` fixed in `41b2b49` — so it is a failure that cannot recur in the shape being counted.
- **`web-e2e-visual` is deliberately excluded, and this is not a lowered bar.** It is
  `continue-on-error` **and** `workflow_dispatch`-only as of `11f5efb`, so a push run's conclusion
  already does not depend on it. Excluding it from the count states the arrangement that exists
  instead of contradicting it. Its own promotion is a separate, later step — and it has a real
  blocker of its own first: the webkit reader baseline is **stale** (see `WK-visual`,
  [48 §3.22d](../48_PlatformParityRegister.md)), so `web-e2e-visual` cannot go green on webkit today
  and could not have been counted even if someone tried.
- **Runs #28 and #29 do not carry forward, even though they were STRICTER.** They were green on the
  7-job shape, which is stronger evidence — but mixing two matrix shapes in one tally is how a
  criterion stops meaning anything, and the count had already reset on #30 regardless. Starting the
  epoch at `41b2b49` costs two runs and keeps the tally readable.
- **Restoring webkit is one word and is NOT required by this gate.** The matrix comment in
  `.github/workflows/web-e2e.yml` records how; doing it would restart the count under the new shape.

**Tally, as of 2026-09-02:**

| Run                     | Trigger             | Four functional jobs | Counts                                                                                                                 |
| ----------------------- | ------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| #30 (`33386725328`)     | push                | webkit red           | reset                                                                                                                  |
| #31 (`33497288725`)     | push                | **cancelled**        | no — a `workflow_dispatch` cancelled it (`concurrency` + `cancel-in-progress`); check `gh run list` before dispatching |
| **#32** (`33497746515`) | `workflow_dispatch` | ✓ ✓ ✓ ✓              | **1 of 3** — same four jobs, run conclusion `success`                                                                  |
| **#33** (`33607209152`) | push                | ✓ ✓ ✓ ✓              | **2 of 3** — first run counted under this section; conclusion `success`, 7.0 min                                       |

**#33 also settled the unreachability argument by measurement rather than by reading the config.**
`web-e2e-visual` reported **`skipped`** — not passed, not failed. The job does not execute on a push
at all, so §6.1's requirement that it be green three consecutive times could never have been
satisfied by any number of green pushes. That was argued from `on:` and `continue-on-error` when
§6.2 was written; the run demonstrated it.

**Remaining promotion steps** (in order): **one** further green push to `develop` → **add** a
`pull_request:` trigger to the `on:` block of
[`web-e2e.yml`](../../.github/workflows/web-e2e.yml) (**lines 14–29**, alongside
`workflow_dispatch:` and `push:`) → mark the **four `web-e2e` jobs** required in branch protection
([docs/17 §4.1](../17_GitWorkflow.md)). `web-e2e-visual` is **not** marked required, for the reasons
above.

> ⚠️ **"Uncomment the trigger" is wrong, and this correction is here because it is the very next
> step anybody takes** (found 2026-09-02, while recording #33). §2's illustrative YAML above carries
> `# pull_request:  # ENABLED at Phase 2 exit`, and both §6.1 and an earlier draft of this section
> inherited the word "uncomment" from it. **The real workflow has never had that line in any form** —
> its `on:` block holds `workflow_dispatch:` and `push:` only, and line 39 (which an earlier draft of
> this section cited) is `jobs:`. So the flip ADDS a trigger; there is nothing to uncomment.
>
> **§2's sample has drifted from the shipped workflow in at least four further ways**, all checked
> against the file rather than counted from memory: `runs-on: ubuntu-latest` vs **`ubuntu-24.04`**
> (`:41`, `:230`); `browser: [chromium, firefox, webkit]` vs **`[chromium, firefox]`** (`:57`); the
> sample's "Start backend, migrate, seed" step still starts the backend **before** migrating, which
> is precisely the §6.1 blocker that was fixed in the workflow; and the sample shows **one job**
> where the workflow has two (`web-e2e-visual`, `:229`). The sample is illustrative and is not
> maintained in lockstep — **when the two disagree, the workflow wins.**

> **Pushing the commit that records this tally is itself the candidate for #34.** Not a curiosity —
> it is the shape of every remaining step, since `web-e2e.yml` carries **no path filters** and so any
> push to `develop` (a docs-only one included) is a counted run.

> ⚠️ **The invariant this section exists to protect.** Whenever the workflow's matrix, triggers or
> `continue-on-error` flags change, this criterion must be re-read — it names jobs, and jobs are
> exactly what those changes move. `#30` cost a run because
> [`playwright.config.ts`](../../e2e/playwright.config.ts)'s `timeout` > `expect.timeout` invariant
> was missing from a design doc rather than from the code. Same class of gap, same section.

---

## 7. Relationship to the existing pipelines

- `ci.yml` (lint/typecheck/unit) stays the fast PR gate; `web-e2e.yml` is the slower integration gate.
- `web-e2e.yml` is a **sibling** of backend `e2e.yml`, not a replacement — they test different layers
  (API contract vs browser workflow) and can run in parallel.
- Deploy workflows (`deploy-staging.yml`, `deploy-production.yml`) may, post-Phase-4, run a **smoke
  subset** (`--grep @smoke`) against the deployed environment as a post-deploy health gate. Design that
  when Phase 4 is stable; out of scope until then.
